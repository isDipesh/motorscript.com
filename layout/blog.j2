{% extends "base.j2" %}

{% block main -%}
    <article class="post">

        <nav class="post_nav">

            <a class="backlink" href="http://motorscript.com">All Posts</a>

            {% if resource.prev_by_time %}
                <a class="prev" title="{{ resource.prev_by_time.meta.title }}"
                   href="{{ content_url(resource.prev_by_time.url) }}">Previous</a>
            {% endif %}

            {% if resource.next_by_time %}
                <a class="next" title="{{ resource.next_by_time.meta.title }}"
                   href="{{ content_url(resource.next_by_time.url) }}">Next</a>
            {% endif %}

            {#<div id="twitter_share">#}
            {#<a href="http://twitter.com/share"#}
            {#    class="twitter-share-button"#}
            {#    data-count="vertical"#}
            {#    data-via="{{ tweet_via }}">Tweet</a>#}
            {#    <script type="text/javascript"#}
            {#        src="http://platform.twitter.com/widgets.js"></script>#}
            {#</div>#}
            {#<div id="facebook_like">#}
            {#<iframe src="http://www.facebook.com/plugins/like.php?href&amp;layout=box_count&amp;show_faces=false&amp;width=450&amp;action=like&amp;font=arial&amp;colorscheme=light&amp;height=65"#}
            {#            scrolling="no"#}
            {#            frameborder="0"#}
            {#            style="border:none; overflow:hidden; width:450px; height:65px;"#}
            {#            allowTransparency="true"></iframe>#}
            {#</div>#}
        </nav>
        <h1 class="title">
            <a href="{{ content_url(resource.url) }}">
                {{ resource.meta.title }}
            </a>
        </h1>
        <time datetime="{{ resource.meta.created.strftime('%Y-%m-%d') }}">
            Posted: {{ resource.meta.created.strftime('%a, %d %b %Y') }}
        </time>

        <div class="tags">
            {% if resource.meta.tags %}
                Tags:
                <ul class="tags">
                    {% for tag in resource.meta.tags %}
                        <li>
                            {{ tag }}{% if not loop.last %},{% endif %}

                        </li>
                    {% endfor %}
                </ul>
                </div>
            {% endif %}
        </div>

        <div class="post-content">
            {% filter markdown|typogrify -%}
                {% mark post -%}
                    {% block post -%}{%- endblock %}
                {%- endmark %}
            {%- endfilter %}
        </div>
    </article>
    <div style="float:right" id="toc"></div>

{%- endblock %}